'use client'

// fport1web doesn't have a user ranks/badges system yet.
// This stub keeps the import contract intact so the mensajes page compiles.
export default function Badges({ uid, ranks, roles, size, className }) {
  return null
}
