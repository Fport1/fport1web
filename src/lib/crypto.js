// Placeholder de cifrado. Hoy: pasa el texto tal cual.
// Luego: implementar AES-GCM con WebCrypto y claves por conversación.

export async function encryptMessage(plainText /*, cid, uid */) {
  return plainText
}

export async function decryptMessage(cipherText /*, cid, uid */) {
  return cipherText
}
